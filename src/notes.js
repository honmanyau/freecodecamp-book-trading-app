export default function notes() {
  console.log('--- SignIn <Link> to register');
  console.log('Error handling from after submitting registration form.')
  console.log('Clear up error messages thrown by firebase.')
}
